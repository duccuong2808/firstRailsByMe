<%= form_for(@blogcr) do |f| %>
  <% if @blogcr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blogcr.errors.count, "error") %> prohibited this blogcr from being saved:</h2>

      <ul>
      <% @blogcr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    Category : <br><%= f.collection_select :catebgs_id, Catebg.find(:all), :id, :namecatebg %>
  </div>
    <div>
    Sort :     <br><%= f.collection_select :sortbgs_id, Sortbg.find(:all), :id, :namesortbg %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :creater %><br />
    <%= f.text_field :creater %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :tag_list, "Tags (separated by commas)" %><br />
    <%= f.text_field :tag_list %>
  </div>
  <div>
    <%= f.file_field :attach %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
